// 获取当前年份
var currentYear = new Date().getFullYear();

// 20年加入前端
var targetYear = 1997;

// 计算经过了多少年
var age = currentYear - targetYear;

const user = {
    age,
    height: 177,
    weight: 62
}
export default user